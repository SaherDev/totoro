'use client';

// RecallFlow renders nothing — results are pushed directly to the thread via pushRecallResults.
// The thinking indicator is handled universally in page.tsx.
export function RecallFlow() {
  return null;
}
